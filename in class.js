// Take a variable with distance in **inches** (in) as input and outputs the same distance but *converted* into **feet** (ft)
// verbs = what the program does

let inches = 5;

let feet = inches/12; 

console.log(`${inches} inches is ${feet} feet`);

// run program in terminal: $ node [fileName.js]